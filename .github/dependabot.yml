version: 2
updates:
  - package-ecosystem: "npm" # or "pip", "maven", etc.
    directory: "/" # location of package manifest
    schedule:
      interval: "daily" # options: "daily", "weekly", "monthly"
    open-pull-requests-limit: 0
    automerge: true # Auto-merge compatible updates
